\usepackage{amssymb, amsmath}%数式
% \usepackage{textcomp}%いろんな記号
\usepackage{mathcomp}
\usepackage[dvipdfmx]{graphicx}
\usepackage{tikz}
% https://tex.stackexchange.com/questions/68890/how-can-i-draw-snake-arrow-in-automata-using-tikz
\usetikzlibrary{quotes, intersections, calc, arrows.meta, patterns, angles, decorations.pathmorphing}
\usepackage{tikz-3dplot}
\usepackage[
	europeanresistors,
	americaninductors,
	oldvoltagedirection,
	straightvoltages
]{circuitikz}

%ここから4つはgnuplot in latex用
%https://event.phys.s.u-tokyo.ac.jp/physlab2024/advent-calendar/22-2/
\usepackage{float}
\usepackage{shellesc}
\usepackage{gnuplottex}
\usepackage{gnuplot-lua-tikz}

\usepackage{wrapfig}
\usepackage{enumerate}
\usepackage{bm}%ボールド（ベクトルとか）
\usepackage{latexsym}
\usepackage[dvipdfmx]{hyperref}%リンク貼り
\usepackage{moreverb}%ソースコード貼る、ハードタブ対応
\usepackage{mathtools}
% \usepackage{physics} %いろいろ問題アリとの噂. \qtyがsiunitxと重複
\usepackage{derivative}
\usepackage{appendix}
% \usepackage[deluxe]{otf}%フォント(見出しとかが変わる（すごく太くなる）)
\usepackage{plext}%部分縦書き 早めにusepackageするべきらしい(https://aminophen.github.io/slide/hytexconf18.pdf)
\usepackage{ifthen}
\usepackage[separate-uncertainty]{siunitx}% 物理量表示など・plextより前に書くとエラーが出る(20240418)

\usepackage{empheq}
\usepackage{shortlst}
\usepackage{silence}
\usepackage{pxjahyper}
\usepackage{fancyhdr}
\usepackage{lastpage}
\usepackage{booktabs}
\usepackage{multirow}
% \usepackage{breqn} % つかいこなせなさそう・こいつ入れると$V_\mathrm{m}$っていう表記がアカンくなる（$V_{\mathrn{m}}$ならいける）
\usepackage{steinmetz} % 角度記号$\angle$の中に文字を入れられる($\phase{#1}$)

\hypersetup{
	setpagesize=false,
	bookmarksnumbered=true,%
	bookmarksopen=true,%
	colorlinks=true,%
	linkcolor=black,
	citecolor=black,
	urlcolor=black,
}
\mathtoolsset{showonlyrefs,showmanualtags}
\mathtoolsset{showonlyrefs=true}
\def\eminnershape{\gtfamily\ebseries}%強調の入れ子の定義({\em {\em abcdefghijk}})

% \renewenvironment{dmath}{\begin{dmath*}}{\end{dmath*}}
% breqnに付随するやつ
% https://tex.stackexchange.com/questions/156433/disabling-enumeration-for-a-document

% \usepackage{mytheorems}
\usepackage{mycommands}

% siunitxの諸設定
\sisetup{input-digits = 0123456789\pi}
\sisetup{list-separator = {\text{, }}}
\sisetup{list-pair-separator = {\text{, }}}
\sisetup{list-final-separator = {\text{, }}}
\sisetup{complex-root-position = before-number}
\sisetup{input-complex-root = ij}
\sisetup{output-complex-root = {\j}}
\DeclareSIUnit\voltampere{VA} % ボルトアンペア（皮相電力）
\DeclareSIUnit\var{var}% バール（無効電力）

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\makeatletter
%節の定義
\renewcommand{\section}{\@startsection{section}{1}{\z@}%インデント
{1.5\Cvs \@plus.5\Cvs \@minus.2\Cvs}%見出しの前の縦空白
{.5\Cvs \@plus.3\Cvs}%見出しの後の縦空白
{\reset@font\LARGE\gtfamily\bfseries}}%見出しのスタイル

%小節の定義
\renewcommand{\subsection}{\@startsection{subsection}{2}{\z@}%インデント
{1.5\Cvs \@plus.5\Cvs \@minus.2\Cvs}%見出しの前の縦空白
{.5\Cvs \@plus.3\Cvs}%見出しの後の縦空白
{\reset@font\large\gtfamily\bfseries}}%見出しのスタイル
%
%小々節の定義
\renewcommand{\subsubsection}{\@startsection{subsubsection}{3}{\z@}%インデント
{1.5\Cvs \@plus.5\Cvs \@minus.2\Cvs}%見出しの前の縦空白
{.5\Cvs \@plus.3\Cvs}%見出しの後の縦空白
{\reset@font\normalsize\gtfamily\bfseries}}%見出しのスタイル

\makeatother

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%簡易な定理環境
